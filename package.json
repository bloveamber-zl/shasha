{
  "name": "shasha-ledger-miniprogram",
  "version": "0.1.0",
  "private": true,
  "description": "沙沙记账微信小程序",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "node scripts/validate-project.js",
    "verify:full": "VERIFY_MODE=full ./init.sh",
    "visual:smoke": "node scripts/visual-smoke.js",
    "visual:smoke:debug": "VISUAL_SMOKE_DEBUG=1 node scripts/visual-smoke.js"
  },
  "devDependencies": {
    "miniprogram-automator": "^0.12.1"
  },
  "engines": {
    "node": ">=18"
  }
}
